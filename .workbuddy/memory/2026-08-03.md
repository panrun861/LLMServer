# 2026-08-03 工作日志

## 中文文档全量对齐更新（全局强制模型特性）
- 背景：此前(08-01)仅更新了 `AITube-PLLM_API设计文档_v1.0.0.md`（v1.1.0，新增 §4.2 全局强制模型 + 修正 3 处事实错误）。用户要求确认"全部中文文档"是否更新，未更新则补齐。
- 盘点 rag-server-docs/AITube-PLLM 下中文文档共 3 份：API设计文档(已更)、需求定义(未更)、PostgreSQL表定义文档(未更)。日文版(3 份)不在中文范围。
- 本轮更新（均 bump 至 v1.1.0，updated_date=2026-08-03）：
  1. `AITube-PLLM_需求定义_v1.0.0.md`：§3.1 新增「全局强制模型」确定需求项（配置后忽略客户端 model 统一路由，须 is_current&is_enabled 否则 404 fail-closed；并明示未配置时标准行为仍生效）；新增 PL-OI-11（需求确定·实现待定）。
  2. `AITube-PLLM_PostgreSQL表定义文档_v1.0.0.md`：OI 区新增 OI-09，联动全局强制模型与表结构（路线A不进DB；路线B需 system_config(key,value JSONB) 表，本表 §6 结构不受影响）。
- 关键判断：3 处事实错误修正（/v1/models 不代理、api_base 不参与路由、切换只查 is_enabled）本质属 API 设计书内部内容，需求定义/表定义未说这些错话，故无需"修正"，仅需补全局强制模型这一新增需求并做 OI 联动。
- 编辑注意：同文件多 Edit 并发时首个编辑报"modified since read"失败，重读后单独补做版本头成功。
- 两份文档为独立嵌套仓库 rag-server-docs，未提交（待用户决定是否 git 操作）。

## 接口完善度解析：/v1/chat/completions 透传能力
- 用户问：模型对话接口是否完善、是否支持关闭 think 及 think token 大小等扩展参数。
- 取证：Pydantic 2.13.4；`inference_gateway.py:121 ChatCompletionRequest(BaseModel)` 无 `extra` 配置 → 默认 `extra="ignore"`，客户端未知字段（enable_thinking/thinking/chat_template_kwargs/max_completion_tokens/repetition_penalty 等）**被静默丢弃**，L289 `model_dump(exclude_none=True)` 不含这些字段 → 转发 LiteLLM 时丢失 → think 关闭/token 预算均无法生效。
- 当前白名单字段（L121-146）：model/messages/temperature/top_p/n/stream/stop/max_tokens/presence_penalty/frequency_penalty/user/tools/tool_choice/response_format/seed/logit_bias/logprobs/top_logprobs/stream_options，日常对话够用。
- 改造方案：A（推荐）`model_config = ConfigDict(extra="allow")` 一行即可自动透传所有扩展字段（L289 不用改）；B 显式声明 thinking 等字段（可控但需维护）。两方案与全局强制模型方案兼容（model 被覆盖、其他字段透传）。
- 其它缺口：`get_usage`(L474) 死代码（被 usage_and_audit.py 覆盖）；审计/计费 model_name 用 body.model(L433/449/461) 应在全局方案落地后改 effective_model；只认旧 `max_tokens` 不认 `max_completion_tokens`。
- 状态：仅分析，未改代码；待用户拍板是否落地方案 A。

## 接口完善落地 + 测试（已交付，commit 627fa47）
- 用户要求：OpenAI /v1/chat/completions 全部完善（不漏参数）+ 结束后测试。
- 改动 `inference_gateway.py`：
  1. `ChatCompletionRequest` 显式补全 OpenAI 官方全部标准参数（store/metadata/max_completion_tokens/modalities/audio/prediction/parallel_tool_calls/service_tier/reasoning_effort/reasoning/functions/function_call 等），并加 `model_config = ConfigDict(extra="allow")` 兜底透传 vLLM/LiteLLM 扩展字段（enable_thinking/thinking/include_reasoning/repetition_penalty/top_k/min_p/typical_p/add_generation_prompt/drop_params）与任意未知字段；加 `ConfigDict`/`Union` import。
  2. **修复流式 0 字节 bug**：原 `async with httpx.AsyncClient(...) as client:` 在 L353 包裹整个逻辑，但 `stream_generator()` 惰性执行，`StreamingResponse` 在 `chat_completions` 返回后才消费生成器 → client 已关闭 → RuntimeError "client has been closed" → 0 字节。重构为流式分支生成器自持 httpx 客户端（`client=None` + `finally: await client.aclose()`），非流式仍用 `async with`。
- 一并部署了此前未提交的中间件修复（signature_verification.py 401→500 / reason_code 溢出）。
- 部署：scp + `docker compose up -d --build app`（项目 /home/ubuntu/aitube-pllm，compose 用 build:. 非挂载，必须重建），容器 aitube-pllm-app-1 已重启。
- 实测（模型 qwen3.6-local，token pllm_sk_tdKYKzz...）：T1 基础对话 200；T2 流式 **修复后 1495B/9 data 事件**（修复前 0B）；T6 max_completion_tokens=20 → usage.completion_tokens=20（证明新字段透传至 vLLM）；T7 repetition_penalty/top_k/x_custom_extra 200（扩展+未知字段透传）；T8 /admin/models 无签名 **401**（修复前 500）；T9 response_format=json_object 200 合法 JSON；T10 tools 200（转发，模型不调用）；T11 seed/parallel_tool_calls 200。
- 注意：enable_thinking/thinking 参数已透传（无 422），但 qwen3.6-local 在 LiteLLM/vLLM 层未启用 thinking（reasoning 恒为 null），故开关无可见效果——属下游模型配置，非 PLLM 代码问题。如需验证 think 开关，需 thinking-enabled 模型变体。
- 残留小问题：`get_usage`(L474) 死代码；审计 model_name 用 body.model（全局强制模型方案落地后改 effective_model）。本次未动。

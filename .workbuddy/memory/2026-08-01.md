# 2026-08-01 工作记录

## 修改 PLLM API 设计书（全局强制模型特性 + 问题修正）
- 文件：`rag-server-docs/AITube-PLLM/AITube-PLLM_API设计文档_v1.0.0.md`（中文版）。版本 1.0.0→1.1.0，updated_date 2026-07-23→2026-08-01。
- 新增 §4.2 全局强制模型（Global Force Model）：覆盖"不论客户端传什么 model 名，统一路由到唯一当前可用模型"。含路线A(env `PLLM_FORCE_MODEL` 静态钉选,推荐)+路线B(DB `system_config`+`PUT/DELETE /admin/global-model` 运行时切换)；与 §6 active-tier 正交；计费/审计记 `effective_model`；强制模型须 is_current&is_enabled 否则 404 fail-closed。
- 同步更新 §4.1 处理链（插入全局强制模型解析步骤）。
- 修正与代码实现不符的事实性错误：
  - §4.0 `/v1/models` 原写成"代理LiteLLM" → 实为直接读 PLLM `models` 表（inference_gateway.py L95-96 `ModelRepo.list_all`），已改。
  - §5 `api_base` 原暗示参与路由/探活 → 代码统一经 `settings.litellm_api_base` 转发，从不读 `model_config["api_base"]`，已澄清 `api_base` 仅为 LiteLLM 配置参考元数据。
  - §6 验证规则原要求"api_base 非空且健康检查通过" → 代码仅查 `is_enabled`（model_management.py:234），已改为仅 is_enabled，健康检查归 LiteLLM。
- §10 未解决事项新增：OI-07(`model-sync-jobs` 文档有但代码未实现/`sync_status` 装饰字段)、OI-08(全局强制模型路线B待定)、OI-09(`model_artifact` 不参与路由需澄清)。
- 另发现但未改（待用户决策）：§4.1 处理链顺序与代码(先查模型再速率/预算)略有差异（功能无碍）；`/v1/usage` 代码重复定义(usage_and_audit.py生效/inference_gateway.py死代码)；签名中间件401→500/reason_code溢出(此前已修代码未部署)；日文版 `API設計書_v1.0.0.md` 未同步本次修改，需并行更新。
- 注意：设计书在独立 repo `rag-server-docs`，不在 aitube-pllm 仓库内；如需提交需分别 git 操作。

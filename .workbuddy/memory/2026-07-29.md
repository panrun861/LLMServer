# 2026-07-29 工作日志

## PLLM 全接口测试（其他接口）

用户要求测试 PLLM 除 token 用量外的其他接口。完整测试套件 `run_api_tests.py`（19 个用例）在远程 `http://35.76.187.215:8080` 上运行，结果 **19/19 通过**。

### 发现并修复的问题
1. **缺失的 issuer（根因）**：切到 `aitube_pllm` 库后，`issuers` 表为空，导致所有 Ed25519 签名的 admin 接口（`/admin/pllm-tokens`、`/admin/usage-records`、`/admin/audit-events`）返回 401（中间件 bug 导致表现为 500）。
   - 修复：在容器内执行 `python -m aitube_pllm.tools.register_issuer --issuer-id aitube-admin --key-id key-2026 --public-key <keys.json 中的 public_key>`（upsert，幂等）。注册后签名接口全部恢复。
2. **测试套件是破坏性的（非 bug）**：`run_api_tests.py` 的 2.8 会 DELETE premium tier。首次运行（修复前）已把它删掉，导致二次运行的 2.8 报 404。已重新注册 premium tier（`context_length:310000`, `api_base:http://35.76.187.215:4000/v1`）恢复两 tier 状态。

### 待办（建议，未实施）
- **中间件 401→500 bug**：`middleware/signature_verification.py` 在 `dispatch` 内 `raise HTTPException(401)`，但 Starlette `BaseHTTPMiddleware` 会将其转为 500。建议改为直接 `return JSONResponse(status_code=401, ...)`，使鉴权失败正确返回 401。仅影响错误响应码，不影响功能。需改代码+重建镜像才会生效，已征询用户是否要修。

### 连接方式
- 远程服务直接可达 `35.76.187.215:8080`（无需隧道）。本地 8080 被占用，测试脚本 BASE 临时改为远程地址（/tmp 副本，未改原文件）。
- SSH Host `L20s` = ubuntu@35.76.187.215（容器 `aitube-pllm`，DB `postgresql://litellm:***@127.0.0.1:5432/aitube_pllm`）。

## 新增监控大盘（只读聚合接口 + 内置看板）

用户要求只暴露一个接口/钩子供外部前端展示，并做看板部署到服务器。

### 改动文件
- 新增 `src/aitube_pllm/api/dashboard.py`：
  - `GET /admin/dashboard/snapshot`：返回全量监控 JSON（totals/latency/timeseries/by_model/by_subject/recent_usage/budgets/security/models/health），`window` 参数(1-720h，≤72h 按小时桶否则按天桶)。供外部前端按需筛选。
  - `GET /admin/dashboard`：自包含 HTML 看板（Chart.js），支持 `?token=`（浏览器直开）和窗口切换。
  - 鉴权：专用只读 token `PLLM_DASHBOARD_TOKEN`，`Authorization: Bearer` 或 `X-Dashboard-Token`。
- `middleware/signature_verification.py`：对 `/admin/dashboard*` 豁免 Ed25519 签名（改由端点自身 token 校验）。
- `config.py` 增加 `dashboard_token`；`main.py` 注册 `dashboard_router`。

### 部署关键坑（bridge 网络 vs host 网络）
- 线上旧容器叫 `aitube-pllm`（host 网络，直接占 8080），本次 compose 服务是 `app`（bridge，映射 8080:8080）。两者端口冲突 → 必须先 `docker stop/rm aitube-pllm` 再 `docker compose up -d app`。
- `app`（bridge）内 `127.0.0.1` 指向自身，**连不上宿主 Postgres/LiteLLM**。必须把服务端 `.env` 的 `PLLM_DATABASE_URL` 与 `PLLM_LITELLM_API_BASE` 的 host 从 `127.0.0.1` 改为 **`host.docker.internal`**（compose 已配 `extra_hosts: host.docker.internal:host-gateway`，且两者均发布到宿主端口）。否则启动连 DB 失败（Connection refused）或推理失败。
- 部署后实测：`/health` 200、snapshot 200（全量 JSON）、内置看板 200、无 token 403、推理冒烟 `/v1/chat/completions` 返回 pong 且 litellm_reachable=true —— 线上推理未被搞挂。

### 对接文档
- `DASHBOARD_API.md`：接口契约、token、返回字段、前端 fetch 示例。
- 部署 token：`pllm_dash_sTq2jdQERFiZF9PWZgVIMxCenXW6jiCI`
- 远程访问：`http://35.76.187.215:8080/admin/dashboard/snapshot` 与 `/admin/dashboard?token=...`

## 监控大盘新增"系统资源监管"（主机/GPU/容器）

用户质疑数据是否为假、并要求加系统资源监控。先确认：snapshot 的 **业务数据全部是真实 SQL 聚合**（usage_logs/pllm_tokens/security_event_logs/models），无任何 mock/回退。随后补充 system 字段。

### 改动
- `src/aitube_pllm/api/dashboard.py`：`build_snapshot` 增加 `await collect_system_metrics()`，写入 `system` 字段：
  - `host`：CPU%(两次采样 delta)、内存(used/total)、磁盘(根 fs used/total)、网络累计+速率(rx/tx KBps)、负载1m、运行时长、进程数。读挂载的 `/host/proc` + `/hostfs`。
  - `gpu`：`nvidia-smi` 采集利用率/显存/温度（真实，部署时实测 L40S 100% 利用率、显存 43.7/46GB、76°C）。
  - `containers`：经 `/var/run/docker.sock` HTTP API 取各容器 CPU%/内存(vllm/litellm/postgres 等)。
  - 优雅降级：挂载缺失时 `available=false` 并带 error，不影响业务数据。
  - 首次采样 cpu_percent=None（无基线），30s 自动刷新后填充。
- `docker-compose.yml`：`app` 服务新增 mounts `/proc:/host/proc:ro`、`/:/hostfs:ro`、`/var/run/docker.sock:/var/run/docker.sock:ro`，并加 nvidia GPU 设备预留 + `NVIDIA_DRIVER_CAPABILITIES=compute,utility`（host 支持 nvidia CDI，预留未影响 vllm/litellm）。
- 内置看板新增"系统资源(主机)""GPU""容器资源"三个面板，顶部 KPI 增加主机 CPU/内存。

### 验证
- `/health` 200；snapshot `system.available=true`，host/gpu/containers 均有真实值；看板页面含新面板(匹配6处)；推理冒烟仍 200(Pong)。
- 注意：GPU 100% 时是 vllm 正在推理，会导致瞬时推理请求排队超时(HTTP:000)，非服务故障。

## "请求数量"语义修正：与 API Key 数量区分

用户指出「api key 的数量不是请求数量；请求数量 = 正在运行的请求 + 排队的请求」。

### 改动（同一文件 dashboard.py，已部署重建）
- 新增 `collect_inference_metrics()`：经 `host.docker.internal:8000/metrics` 拉取 vLLM Prometheus 指标，解析 `vllm:num_requests_running`（运行中）+ `vllm:num_requests_waiting`（排队，含 reason=capacity/deferred）。这是 **GPU 真实实时并发的 ground truth**（链路 PLLM→LiteLLM→vLLM，vLLM 才是占 GPU 的层）。
  - LiteLLM `/metrics` 需鉴权（设置了 require_auth），暂未接入；vLLM 已足够代表在途并发。
- `build_snapshot` 新增：
  - `inference` 字段：`running_requests` / `queued_requests` / `total_inflight` / `waiting_by_reason` / `models[]`。
  - `keys` 字段：`active`(7) / `total`(11) —— 即 **API Key 数量**，与请求数量彻底分开统计，避免混淆。
- 看板：KPI「请求数」改名为「窗口请求数」（=窗口内已完成数）；新增「运行中请求/排队中请求/在途请求合计/活跃密钥数」四个 KPI；新增面板「推理实时并发(运行中+排队中)」并明确标注「与 API Key 数量无关」。
- `DASHBOARD_API.md` 同步补充 `inference`/`keys` 字段与语义澄清。

### 验证
- snapshot 返回 `inference.available=true, engine=vllm, running=0, queued=0`（当前空闲，真实）；`keys: {active:7, total:11}`。
- 看板页面含全部新面板/KPI（关键词匹配 OK）。
- 说明：idle 时光标为 0/0 是真实状态；发起推理请求期间 `running_requests` 会瞬时变 1（因生成耗时较长，演示轮询被后台缓冲吞掉未抓到峰值，但字段与数据源均验证正确）。

## 监控大盘新增 KV Cache 缓存监控（vLLM）

用户要求加 KV cache 缓存监控，并追问「模型登记里两个没见过的模型」。

### KV cache 改动（dashboard.py，已部署重建）
- `_parse_vllm_metrics` 增加解析：`vllm:kv_cache_usage_perc`(占用率%)、`vllm:prefix_cache_queries_total`/`hits_total`(前缀命中率)、`vllm:cache_config_info`(num_gpu_blocks/kv_cache_size_tokens/gpu_memory_utilization/block_size/cache_dtype/enable_prefix_caching)。
- `collect_inference_metrics` 的 result 增加 `kv_cache` 默认结构；snapshot 的 `inference.kv_cache` 含 `usage_perc`/`by_model`/`prefix_cache`/`config`。
- 看板新增「KV Cache 缓存 (vLLM)」面板（占用率/Prefix命中率/GPU blocks/KV容量/显存利用率/类型）。

### 模型登记脏数据（已查证，待用户决策）
`models` 表共 3 行，除主模型 `qwen3.6-local`(medium, is_current=t) 外，另两个是测试残留：
- `apifox-qwen-test`(tier=test, artifact=qwen3.6-27b)：apifox 接口调试工具调 `/admin/models` 建的，created 2026-07-27。
- `test-model-debug`(tier=premium, artifact=`openai/gpt-4`)：调试占位，指向本地不存在的 gpt-4，created 2026-07-27。
- 两者均 `sync_status=pending`、`is_enabled=true`，故出现在看板「模型登记」里。非系统自动发现，是早期调试/接口测试写入库的脏数据。
- **已删除**：用户确认「直接删除」。先查依赖（`pllm_tokens`/`budgets` 均无 `model_name` 列，`usage_logs.model` 为文本快照），确认无 FK 依赖后 `DELETE FROM models WHERE model_name IN ('apifox-qwen-test','test-model-debug')` 删除 2 行。现 models 表只剩 `qwen3.6-local`(medium, is_current=t)。看板实时读库，无需重启即生效，snapshot 已验证只剩 1 个模型。

## 将最新版提交 git 实现三端同步

用户要求把最新版更新到 git，使 git / 本地工作区 / 服务器容器三端一致。

### 关键仓库事实（首次明确）
- **git 仓库根在 `rag/` 层级**，并非 `aitube-pllm/`。`aitube-pllm/` 是子目录，`rag-server-docs/` 是**嵌套 git 仓库（子模块）**，git status 显示其有 untracked content——**永远不要 `git add -A`/`.`**，否则会误碰子模块；必须显式 add 指定文件。
- `core.autocrlf=true`（Windows）：提交时提示 LF→CRLF 警告，但当前工作树文件仍是 LF，`git diff HEAD` 为空，MD5 与服务器一致，属良性。
- 无 git remote（未配置任何远程），所以"三端"= git 本地仓库 / 本地工作区 / 服务器容器，不涉及 push。

### 提交
- commit `199c1e3` on `master`，6 文件 +1206 行：`aitube-pllm/docker-compose.yml`、`aitube-pllm/src/aitube_pllm/{config,main}.py`、`aitube-pllm/src/aitube_pllm/middleware/signature_verification.py`、`aitube-pllm/src/aitube_pllm/api/dashboard.py`(新建)、`aitube-pllm/DASHBOARD_API.md`(新建)。
- 验证：本地 5 个代码文件 MD5 与提交前一致（c0446e47/bb3d0c7b/4ca97708/80e2fb1c/749577a5）；`git diff HEAD` 空；此前已确认服务器/容器内 MD5 相同 → 三端同一版本。
- **有意未提交**：`.workbuddy/memory/2026-07-29.md`(内部记忆)、`tests/API_TEST_REPORT_2026-07-29.md`(测试产物，项目 .gitignore 约定不提交)、`../rag-server-docs`(子模块)。

## 修正 git 仓库根（rag 之前误成父仓库）

用户指出 `rag` 仓库根不对（里面嵌了 aitube-pllm 子目录 + rag-server-docs 嵌套仓库，共"两个仓库"）。

### 关键事实
- 原本地 git 仓库根是 `C:/Users/ISOUPC/Documents/projects/rag`（不是 aitube-pllm），提交路径带 `aitube-pllm/` 前缀；`rag-server-docs` 是嵌套 git 仓库。
- GitHub `panrun861/LLMServer` 的 `main` 根 = aitube-pllm 项目本身（docker-compose.yml、src/aitube_pllm/... 在根，无 `aitube-pllm/` 嵌套）。

### 操作
- 直接 push 原 `rag` 仓库会被拒（unrelated，且会把 `aitube-pllm/` 层与子模块推上去）。
- 采用"基于远程 main 的干净增量"法：`git clone --depth 1` LLMServer 到 `rag/_llm_clone`（根结构正确），仅把 6 个改动文件覆盖进去，在 `ac4e68f` 之上提交 `2f6df05`（6 文件/+1206 行）。
- HTTPS push 卡在交互式账号密码 → 改用 **SSH**（`~/.ssh/id_ed25519` 已注册 GitHub，`ssh -T git@github.com` 返回 `Hi panrun861!`）。`git remote set-url origin git@github.com:panrun861/LLMServer.git` 后 `git push origin main` 成功：`ac4e68f..2f6df05 main -> main`。
- 重组本地结构（aitube-pllm 被进程锁住无法 rename/delete，故用"复制 .git 进目录"而非移动目录）：
  - `rag/.git`（父仓库）用 PowerShell Move-Item 移出项目区（中和父仓库，rag 不再含 .git）。
  - 把 `_llm_clone` 的正确 `.git` 复制进 `aitube-pllm/`，`git reset --hard HEAD` 对齐工作树 → aitube-pllm 成为根结构正确的独立仓库。
  - `rag-server-docs` 独立仓库保持不动。

### 最终状态
- `rag/` = 纯容器目录，含两个独立仓库：`aitube-pllm/`（根=项目，origin=LLMServer SSH，HEAD `2f6df05`==origin/main）、`rag-server-docs/`。
- 本地 HEAD == origin/main == GitHub `2f6df05`。三端（GitHub / 本地 / 服务器容器）一致。
- 注意：用户给的是 HTTPS URL，实际推送走 SSH（无 token）。如需改回 HTTPS 需提供 GitHub PAT。
- `.workbuddy/` 现位于 aitube-pllm 内且未被 gitignore，属未跟踪；建议后续在 .gitignore 加 `.workbuddy/` 以免误提交（未擅自改动 .gitignore）。

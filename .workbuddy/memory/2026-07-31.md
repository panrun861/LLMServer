# 2026-07-31 工作记录

## 远程服务器(L20s) 清理多余 Docker 镜像
- 经 SSH(L20s=ubuntu@35.76.187.215) 取证：镜像 19 个，仅 8 个被容器引用，悬空(dangling)=0，12.2GB 可回收。
- **第一批(明确安全)**：删 9 个镜像 + 1 个 Exited(137) 死容器 `aitube-pllm-failed-ac4e68f`。含 `vllm:v0.20.1`(旧版,共享层实际释放较少)、`aitube-pllm:1.0.0`/`ac4e68f`(旧 app 构建)、被弃用 `cadvisor`、`prometheus/node-exporter:latest`(重复标签)、`postgres:16-alpine`、未用 `docker.litellm.ai/...litellm`、`grafana`。
- **第二批(用户选"全部删除")**：删剩余 11 个空闲镜像 = op_bot 两个 `new-telegram-bot-openhands:1`/`ghcr.io/openhands/agent-canvas:latest`(各 6.32GB)、`pi-*`/`new-bot2`/`nginx:alpine`、基础镜像 `nvidia/cuda:12.4.1`/`nvcr.io/nvidia/cuda:12.1.0`/`node:22-alpine`/`alpine`。
- **结果**：镜像 19→8，全部在用(8/8, 0 可回收)，镜像存储 44.82GB→30.76GB。
- ⚠️ 影响：`docker images` 已无 op_bot/pi/bot/nginx 镜像；若需重新部署 op_bot 或这些 bot，须重新拉取(openhands 两个约 12.6GB)。基础镜像已删，后续 `docker build` 若依赖 node/alpine/nvidia-cuda 标签需重新拉 base。
- 未处理（待用户决定是否清理）：Build Cache 10.37GB(177 条, 0 active, 可 `docker builder prune -f` 安全回收)；Local Volumes 30.93GB 可回收(4/8 未用, 含数据风险未动)；Containers 全在用。

## 客户机器最小化清理（仅保留 PLLM 所需）
- 用户明确："多余的都删掉，这是客户机器，我只需要放 PLLM 需要的"。
- 取证：8 个镜像全部 PLLM 相关(app/vLLM/LiteLLM/Postgres/Prometheus监控栈)，无可删镜像。
- 删非 PLLM 残留：
  - 容器：`aitube-pllm-postgres-1`(Restarting 冗余库，app 实际走 stack 的 postgres) → `docker rm -f`
  - 网络：`ubuntu_default`、`op-bot-windows_default` → `docker network prune -f`
  - 构建缓存：10.37GB(177 条,0 active) → `docker builder prune -f`
  - 卷(显式按名删，`volume prune` 因 compose 命名卷边缘情况回收 0B)：`aitube-pllm_postgres_data`(冗余库垃圾)、`op-bot-windows_litellm-db-data`、`stack_openhands_config`、`stack_openhands_workspace`、`ubuntu_hf_cache`
- **最终状态(全 0B 可回收)**：
  - Images 8/8 active(30.76GB，均为 PLLM)
  - Containers 8/8 active(704.8MB，均为 PLLM：app/vllm/litellm/postgres/pllm-prometheus+3 exporter)
  - Volumes 3/3 active(68.57GB)：`stack_pgdata`(真实库数据)、`stack_hf_cache`(vLLM 模型缓存)、`monitoring_prometheus_data`
  - Build Cache 0
  - 网络仅剩 PLLM 三张：`aitube-pllm_default`/`monitoring_monitoring`/`stack_default`(+host/bridge/none)
- 注：此前(上一轮)"全部删除"已删 op_bot/pi/bot/基础镜像，本轮进一步清掉非 PLLM 的网络/卷/构建缓存与冗余 postgres 容器。机器现仅含 PLLM 运行所需。

## 全局模型强制路由 方案书（仅方案，未落地代码）
- 需求：新增全局模型参数，不论 OpenAI 接口传入什么 model 名，统一路由到唯一当前可用模型。用户明确"目前只需要写方案书"。
- 取证结论：`inference_gateway.py` L251 按 `body.model` 查白名单、L289 原值转发 LiteLLM；**`model_artifact` 字段当前未参与转发**（技术债）；现有 `PUT /admin/models/{model_name}/active-tier`(`activate_tier`, repos.py:320) 仅切换 per-model 的 active tier，非"忽略客户端模型名"。
- 已确认决策：D1 强制模型须为当前可用模型(is_current&is_enabled)；D2 `/v1/models` 仍返回全部已启用；D3 新机制叠在 is_current 之上、正交。
- 方案书产出：`docs/global-model-override-proposal.md`。路线A=env `PLLM_FORCE_MODEL` 静态钉选(推荐)；路线B=DB `system_config`+`PUT/DELETE /admin/global-model` 运行时动态切换(增强)。改动点集中在 config.py 加 `force_model` 字段 + inference_gateway.py 用 `effective_model = settings.force_model or body.model` 替换 L251/255/262/289/362/373/399/422/433/449/461。
- 待确认：选 A 还是 A+B；`PLLM_FORCE_MODEL` 具体取值(需先 SSH 确认 LiteLLM 当前可用模型别名)。
